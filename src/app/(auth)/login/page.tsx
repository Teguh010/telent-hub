import LoginForm from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Login - Talent Hub',
  description: 'Sign in to your Talent Hub account',
};

export default function LoginPage() {
  return <LoginForm />;
}
