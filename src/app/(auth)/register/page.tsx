import RegisterForm from '@/components/auth/RegisterForm';

export const metadata = {
  title: 'Register - Talent Hub',
  description: 'Create a new Talent Hub account',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
